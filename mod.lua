function data()
	return {
		info = {
			minorVersion = 0,
			severityAdd = "NONE",
			severityRemove = "NONE",
			name = _("Demo Clock"),
			description = "The 'uiUpdate' callback in a game script is used to add a clock to the game.",
		},
 }
end